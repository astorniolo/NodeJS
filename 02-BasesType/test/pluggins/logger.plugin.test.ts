//probar que se llamen los metodos que esperan

import { build_logger } from "../../src/pluggins";

// nec que se haya llamado y con sus argumentos
describe('pluggins/logger.plugin.ts', ()=>{
    test('buildlogger should return a funion logger', ()=>{
        const logger=
    })
});